const char ch = 'c';
char alpha;
char toLower(char x)
{
	if (x<65) {
		printf("Please check your letter.    ");
		return (0);
	}
	else {
		if (x>90) {
			printf("Please check your letter.    ");
			return (0);
		}
		else {
			x = x + 32;
		}
	}
	return (x);
}

void main() {
    scanf(alpha);
	alpha = toLower(alpha);
if(alpha){	
printf("lowercase:    ",alpha);}else{}
}